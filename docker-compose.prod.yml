version: '3'

services:
  app:
    container_name: fast-api-app
    env_file: .env
    build: .
    command: uvicorn src.api_report_analysis:app --host 0.0.0.0 --port 8000 --reload
    restart: always
    ports:
      - "8001:8000"

